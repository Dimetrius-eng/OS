# Лабораторна робота №9
## Дисципліна: Операційні системи
## Тема: “Захист системи та користувачів у Linux. Створення користувачів та груп”
### Виконав: студент групи РПЗ-33, Руденко Дмитро

---

<div align="justify"> 
  
### Мета роботи:  
<span>1. Отримання практичних навиків роботи з командною оболонкою Bash.</span>     
<span>2. Знайомство з базовими діями при створенні нових користувачів та нових груп користувачів.</span>   

### Матеріальне забезпечення занять:  
<span>1. ЕОМ типу IBM PC.</span>    
<span>2. ОС сімейства Windows та віртуальна машина Virtual Box (Oracle).</span>    
<span>3. ОС GNU/Linux (будь-який дистрибутив).</span>   
<span>4. Сайт мережевої академії Cisco netacad.com та його онлайн курси по Linux.</span>

### Завдання для попередньої підготовки.

#### 1. *Прочитайте короткі теоретичні відомості до лабораторної роботи та зробіть невеликий словник базових англійських термінів з питань призначення команд та їх параметрів.

| № | Слово | Пояснення |
| :--- | :--- | :--- |
| 1 | **sudo** | Команда - дозволяє звичайному користувачу виконувати команди з правами адміністратора (root) |
| 2 | **su** | Команда - дозволяє перемкнутися на обліковий запис іншого користувача або root |
| 3 | **id** | Команда - виводить ідентифікатор користувача (UID) та ідентифікатори груп (GID), до яких він належить |
| 4 | **who** | Команда - відображає список користувачів, які зараз увійшли в систему |
| 5 | **last** | Команда - зчитує історію входів з файлу /var/log/wtmp і відображає всі входи та перезавантаження |
| 6 | **groupadd** | Команда - використовується суперкористувачем для створення нової групи |
| 7 | **groupmod** | Команда - використовується для зміни імені або GID існуючої групи |
| 8 | **getent** | Команда - відображає локальні та мережеві групи або користувачів (бази даних) |
| 9 | **UID (User ID)** | Термін - унікальний числовий ідентифікатор користувача в системі Linux |
| 10 | **GID (Group ID)** | Термін - унікальний числовий ідентифікатор групи користувачів |

#### 2. Вивчіть матеріали онлайн-курсу академії Cisco “NDG Linux Essentials”:

<span>- Chapter 15 - System and User Security</span>  
<span>- Chapter 16 - Creating Users and Groups</span>

#### 3. Пройдіть тестування у курсі NDG Linux Essentials за такими темами:  

<span>- Chapter 15 Exam</span>     
<span>- Chapter 16 Exam</span> 

#### 4. На базі розглянутого матеріалу дайте відповіді на наступні питання:

<blockquote>

**4.1. Розкрийте поняття UPG, коли їх доцільно використовувати?**

**UPG (User Private Group)** — це приватна група користувача. У деяких дистрибутивах під час створення нового облікового запису автоматично створюється група з таким самим іменем, і цей користувач є її єдиним учасником. Це доцільно використовувати для кращої гнучкості управління правами доступу, оскільки дозволяє користувачеві встановлювати права за замовчуванням так, щоб ніхто інший не мав доступу до його файлів, але при цьому легко ділитися каталогами з іншими спільними групами (використовуючи SGID).

**4.2.** ***Якими командами можна створити групи користувачів? Наведіть приклади**

Для створення груп використовується команда `groupadd`.

Приклади:  

- `sudo groupadd developers` (створює групу з автоматичним присвоєнням наступного вільного GID).  
- `sudo groupadd -g 1005 research` (створює групу зі строго заданим GID 1005).

**4.3.** ****Якими командами можна змінити налаштування груп користувачів? Наведіть приклади**

Для зміни налаштувань використовується команда `groupmod`.

Приклади:  

- `sudo groupmod -n managers sales` (змінює назву групи з "sales" на "managers").  
- `sudo groupmod -g 1020 managers` (змінює GID групи "managers" на 1020).

</blockquote>

#### 5. Підготувати в електронному вигляді початковий варіант звіту:

<span>- Титульний аркуш, тема та мета роботи</span>  
<span>- Словник термінів</span>  
<span>- Відповіді на п.4.1 та п.4.5 з завдань для попередньої підготовки</span>

## Хід роботи

#### 1. Початкова робота в CLI-режимі в Linux ОС сімейства Linux:
  
**1.1. Запустіть операційну систему Linux Ubuntu. Виконайте вхід в систему та запустіть термінал (якщо виконуєте ЛР у 401 ауд.).**

**1.2. Запустіть віртуальну машину Ubuntu_PC (якщо виконуєте завдання ЛР через академію netacad)** 

**1.3. Запустіть свою операційну систему сімейства Linux (якщо працюєте на власному ПК та її встановили) та запустіть термінал.**

<blockquote>
  
Під час виконання роботи я буду використовувати свою, встановлену під час виконання Work-case 2, операційну систему сімейства Linux:

<img width="1920" height="955" alt="зображення" src="https://github.com/user-attachments/assets/5404b044-0153-4a91-8560-6e9980e3dc27" />

</blockquote>

#### 2. Опрацюйте всі приклади команд, що представлені у лабораторних роботах курсу NDG Linux Essentials - Lab 15: System and User Security та Lab 16: Creating Users and Groups. Створіть таблицю для опису цих команд.

| Назва команди | ЇЇ призначення та функціональність |
| :--- | :--- |
| `su -` | Переключити користувача на користувача root (запропонує ввести пароль) |
| `id` | Підтвердити особу нового користувача |
| `exit` |  Повернути до початкової оболонки (і початкового облікового запису користувача) |
| `grep sysadmin /etc/passwd` | Переглянути запис для вашого облікового запису системного адміністратора |
| `sudo head -3 /etc/shadow` | Переглянути кілька перших рядків файлу /etc/shadow, файлу, який містить зашифровані паролі користувачів та інформацію про їх старіння |
| `getent passwd sysadmin` | Отримати інформацію про системного адміністратора |
| `man 5 passwd` | Переглянути документацію щодо полів у файлі /etc/passwd |
| `who` | Отримати поточний список користувачів у системі |
| `w` | Отримати більш детальний перегляд користувачів, які зараз перебувають у вашій системі |
| `last` | Переглянути файл /var/log/wtmp, який зберігає журнал усіх користувачів, які входили та виходили з системи |
| `groupadd -r research` | Створити групи під назвою дослідження |
| `getent group research` | Отримати інформацію про нову дослідницьку групу |
| `grep sales /etc/group` | Отримати інформацію про нову групу продажів |
| `groupmod -n clerks sales` | Змінити назву групи продажів |
| `groupmod -g 10003 clerks` | Змінити GID для групи |
| `grep clerks /etc/group` | Перевірити внесені вище зміни |
| `groupdel clerks` | Видалити групу клерків разом із назвою групи |
| `useradd -D` | Переглянути значення за замовчуванням, які використовуються |
| `nano /etc/default/useradd` | Змінити значення CREATE_MAIL_SPOOL у файлі /etc/default/useradd за допомогою текстового редактора nano |
| `usermod -aG research sysadmin` | Додати дослідницьку групу як допоміжну групу для користувача системного адміністратора |
| `passwd student` | Установити пароль, netlab123, для користувача студента ( потрібно ввести пароль двічі, а потім знову переглянути запис тіньового файлу для користувача студента |
| `userdel -r student` | Видалити обліковий запис студента та видалити домашній каталог користувача |

#### 3.  Виконайте практичні завдання у терміналі (продемонструйте скріншоти):

<span>- виведіть інформацію про поточного користувача різними способами (підказка використовуйте команди id та grep);</span>  

<img width="805" height="112" alt="1" src="https://github.com/user-attachments/assets/47efa165-ca9c-484c-972f-5f8f373ccb4a" />

<br>

<span>- *попрактикуйте в терміналі команди last, w та who. Порівняйте результати виводу кожної команди, які деталі відсутні в кожній із команд порівняно з іншими?</span>  

<img width="804" height="291" alt="2" src="https://github.com/user-attachments/assets/611158c1-ffab-463f-9f1a-af737cc60010" />

<br>

- `who`: Відображає базову інформацію — лише імена користувачів, які зараз в системі, їхні термінали та час входу. Відсутні деталі про те, що саме вони роблять.  
- `w`: Дає найповнішу картину про поточний стан. Вона показує те саме, що й who, але додатково відображає навантаження на систему (load average), час бездіяльності користувача (IDLE) та конкретну команду/процес, яку він зараз виконує (WHAT).  
- `last`: На відміну від попередніх двох, показує не лише поточних користувачів, а й історію входів (тих, хто вже вийшов із системи), а також системні події, такі як перезавантаження (reboot) та час роботи сесій.

<span>- *створіть дві нові групи користувачів - super_admins, noob_users та good_students, визначте їх ідентифікатори;</span>  

<img width="798" height="178" alt="3" src="https://github.com/user-attachments/assets/8b531b92-bf26-43ab-8778-6d19e2f1284b" />

<br>

<span>- *для кожного члену Вашої команди за допомогою терміналу створіть нового користувача (якщо працюєте самі, то просто трьох довільних користувачів), не забудьте після створення нового користувача одразу задати йому пароль;</span>  

<img width="794" height="329" alt="4" src="https://github.com/user-attachments/assets/1b79dc26-cce1-4adb-9605-ec8a472adcf9" />

<br>

<span>- **додайте нових користувачів у створені Вами нові групи таким чином, щоб у групах super_admins та noob_users було по 2 користувачі, один з яких є в обох групах, у групу good_students додайте всіх трьох користувачів;</span>  

<img width="804" height="69" alt="5" src="https://github.com/user-attachments/assets/92a1f516-ad8f-4f96-8a3c-7ec65f1de844" />

<br>

<span>- **перегляньте інформацію про групи, та які користувачі до них входять, поясніть що ви бачите;</span>  

<img width="803" height="87" alt="6" src="https://github.com/user-attachments/assets/bf9ab993-6c2c-4faf-9652-228177de2bc0" />

<br>

У виводі ми бачимо рядки формату назва_групи:x:GID:список_користувачів. Символ x означає, що пароль групи зберігається в іншому захищеному файлі (/etc/gshadow) або не використовується. GID — це ідентифікатор. Далі через кому перелічені імена користувачів (наприклад, alice,bob,charlie), для яких ця група є додатковою.

<span>- **видаліть першого створеного вами користувача, перегляньте чи залишиться інформація про нього в групах, де він перебував;</span>

<img width="798" height="111" alt="7" src="https://github.com/user-attachments/assets/cd715eea-d510-45fd-9330-55aceafddbb2" />

<br>

Параметр `-r` видаляє користувача разом із його домашнім каталогом. При перевірці груп ми бачимо, що інформація про `alice` не залишилася в групах — система автоматично прибрала її зі списків `super_admins` та `good_students`.

<span>- **видаліть другого користувача, перегляньте чи залишиться інформація про нього в групах, де він перебував;</span>  

<img width="802" height="130" alt="8" src="https://github.com/user-attachments/assets/43da789a-1597-49c6-b6f7-88600ee6a667" />

<br>

Після видалення `bob` його ім'я також автоматично зникло з усіх пов'язаних груп. Група `super_admins` тепер стала повністю порожньою.

<span>- **видаліть третього користувача, перегляньте чи залишиться інформація про нього в групах, де він перебував;</span> 

<img width="792" height="107" alt="9" src="https://github.com/user-attachments/assets/ea99882d-517c-444f-911e-b05d9da98f64" />

<br>

Після видалення останнього користувача (`charlie`), списки членів у групах `noob_users` та `good_students` також стали порожніми.

<span>- **перегляньте інформацію про існуючі групи користувачів;</span>  

<img width="803" height="92" alt="10" src="https://github.com/user-attachments/assets/783caa11-f2a5-4c9f-a66e-8de6b734c1c5" />

<br>

<span>- **видаліть створені Вами групи користувачів;</span>  

<img width="800" height="67" alt="11" src="https://github.com/user-attachments/assets/b3374f97-486c-4f57-8883-19dbc5f79a7a" />

<br>

<span>- **перегляньте інформацію про існуючі групи користувачів.</span>

<img width="773" height="54" alt="12" src="https://github.com/user-attachments/assets/ae7c37ff-1562-41ac-85ec-c89d98d2a450" />

### Контрольні запитання

**1. Чому в конфігураційних файлах паролі не зберігається в явному вигляді?**  

Задля безпеки. Якщо файл `/etc/passwd` або `/etc/shadow` буде перехоплений зловмисником, він не зможе одразу використати паролі. Зберігаються лише їхні криптографічні хеші.

**2. Чому не рекомендується виконувати повсякденні операції, використовуючи обліковий запис root?**  

Це небезпечно, оскільки одна випадкова помилка (наприклад, неправильна команда видалення) може знищити всю систему. Крім того, будь-яка програма (наприклад, браузер), запущена з-під root, матиме абсолютний доступ до системи, що робить її вразливою до вірусів та зломів.

**3.** ***У чому відмінність механізмів отримання особливих привілеїв su і sudo?**  

`su` (switch user) повністю перемикає ваш сеанс на обліковий запис `root`, і для цього вам потрібно знати пароль самого root. `sudo` дозволяє виконати лише одну конкретну команду з правами root, залишаючись у своєму обліковому записі, і для цього ви вводите свій власний пароль. Крім того, дії через `sudo` логуються системою.

**4.** ***Чому домашній каталог користувача root не розміщено в каталозі /home?** 

Каталог `/home` часто монтується на окремий логічний розділ або мережевий диск. Якщо під час завантаження системи станеться помилка і `/home` не змонтується, адміністратор (root) повинен мати змогу увійти в систему, щоб усунути проблему. Тому каталог `/root` знаходиться на кореневому розділі (`/`), який доступний завжди.

**5.** ***Для чого використовується команда getent?**  

Команда `getent` використовується для отримання записів з адміністративних баз даних (наприклад, `passwd`, `group`, `hosts`). Її головна перевага перед `cat /etc/group` полягає в тому, що вона показує як локальні облікові записи, так і мережеві (наприклад, через LDAP або Active Directory).

**6.** ***Як можна змінити пароль користувача?** 

За допомогою команди `passwd`. Суперкористувач може змінити пароль будь-кого (наприклад, `sudo passwd bob`), а звичайний користувач — лише свій (ввівши `passwd`).

**7.** ****Яким чином можна видалити існуючі групи користувачів? Чи залишиться інформація про них десь у системі?**

За допомогою команди `groupdel` (наприклад, `sudo groupdel mygroup`). Якщо видалити групу, її запис зникає з /etc/group та /etc/gshadow. Однак файли, які належали цій групі, не видаляються; замість імені групи у їхніх властивостях буде відображатися "осиротілий" числовий GID (Group ID).

**8.** ****Яке призначення команди chage?** 

Команда `chage` використовується для управління політикою старіння паролів (password aging). Вона дозволяє встановити, через скільки днів користувач зобов'язаний змінити пароль, коли обліковий запис буде заблоковано та коли попереджати про необхідність зміни пароля.

**9.** ****Які параметри команди usermod ви вважаєте найбільш використовуваними?**

- `-aG` — додає користувача до додаткових груп (не видаляючи з існуючих).

- `-l` — змінює логін (ім'я) користувача.

- `-L` (Lock) та `-U` (Unlock) — блокує та розблоковує обліковий запис.

- `-s` — змінює командну оболонку (shell) за замовчуванням.

- `-d` — змінює шлях до домашнього каталогу.

## Conclusions

In this laboratory work, I gained practical skills in user and group management within the Linux operating system, focusing on system security principles. I explored the differences between administrative accounts and standard user accounts, understanding why direct root access is discouraged and how the sudo mechanism mitigates security risks. During the practical session, I successfully used CLI tools to create new users (useradd), define passwords (passwd), and organize them into specialized groups (groupadd, usermod). Furthermore, I analyzed user information using commands like id, w, and last, and examined how deleting a user or a group affects the system's databases and file ownership. This practice significantly enhanced my understanding of Linux access control and account administration.











